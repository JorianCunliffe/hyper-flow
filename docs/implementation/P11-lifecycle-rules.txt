
> hyperflow@0.0.0 test:rules
> npx --yes firebase-tools emulators:exec --project hyper-flow-a459b --only database --config firebase.test.json "node tests/e2e/rules.test.mjs"

i  emulators: Starting emulators: database
i  database: Database Emulator logging to database-debug.log
i  Running script: node tests/e2e/rules.test.mjs

Membership cannot be self-assigned
  PASS a user cannot write their own membership record
  PASS an outsider cannot add themselves to another organization
  PASS membership writes are backend-only, including by owners

Invite records are backend-only
  PASS a client cannot read an invite record
  PASS a client cannot create an invite record
  PASS a client cannot consume an invite directly

Tenant isolation follows server-created membership
  PASS anonymous users cannot read tenant data
  PASS another tenant cannot read tenant data
  PASS a server-created member can read tenant data
  PASS a server-created member can write tenant data

Private and operational trees
  PASS user profiles are private
  PASS the event inbox is backend-only
  PASS clients cannot forge event inbox records
  PASS triage_items reads are API-only, even for tenant members
  PASS triage_items cannot be forged across tenants
  PASS schedules reads are API-only, even for tenant members
  PASS schedules cannot be forged across tenants
  PASS schedule_runs reads are API-only, even for tenant members
  PASS schedule_runs cannot be forged across tenants
  PASS scheduler_health reads are API-only, even for tenant members
  PASS scheduler_health cannot be forged across tenants
  PASS service_setup_drafts reads are API-only, even for tenant members
  PASS service_setup_drafts cannot be forged across tenants
  PASS communication_cursors reads are API-only, even for tenant members
  PASS communication_cursors cannot be forged across tenants
  PASS ask_resolutions reads are API-only, even for tenant members
  PASS ask_resolutions cannot be forged across tenants
  PASS communication_delivery reads are API-only, even for tenant members
  PASS communication_delivery cannot be forged across tenants
  PASS server activity is backend-only

30 passed, 0 failed

+  Script exited successfully (code 0)
i  emulators: Shutting down emulators.
i  database: Stopping Database Emulator
!  Database Emulator has exited upon receiving signal: SIGINT
i  hub: Stopping emulator hub
i  logging: Stopping Logging Emulator
