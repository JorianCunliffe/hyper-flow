# Configurable SMS and voice continuity

Settings → Agent & Connections → Conversation continuity exposes prior-communications use, a shared conversation prompt and additional SMS/voice instructions. Agent-profile saves use existing administrator checks and REST persistence. Prompts are bounded; blank values clear them. Defaults preserve prior grants, email draft-only policy and action approvals.

Both answer paths receive transient Communications evidence scoped to the caller and selected project. Source IDs, semantic thread IDs, channels, directions and timestamps remain attached. Evidence excludes private/ineligible sources, is bounded/non-exhaustive, and reports unavailable/stale/disabled history. SMS replies explicitly bind the inbound semantic thread and incoming SMS is hydrated before the durable agent job is queued. Voice retains its contact/line prompt and adds shared/channel instructions plus scoped history, without enabling unrestricted legacy history.

Validation: 590 HyperFlow unit tests, typecheck/build; new cases cover prompt save/clear/bounds, history disable/revocation, cross-channel source projection, privacy and source budgets. Phase07 cross-service acceptance now asserts real REST evidence reaches voice context and channel-specific prompts are selected. All cross-service acceptance and verification jobs passed in CI, including on merged main 5d308677113d25d77cab9749259215b95ee87af9. Production HyperFlow is READY on that revision. Communications v2.8.1 reports the expected runtime fingerprint 6405fedcc3ae. The shared and channel prompts were saved through the live UI and survived reload. A signed production context probe initially returned 403 because the CEO agent had no voice/SMS service identity assigned. After explicit user approval, +61468072278 was saved through the UI. The repeated signed probe selected project 1787628008985, returned current SMS evidence and confirmed both saved shared and voice prompts. One cold request exceeded the existing 4.5-second bridge budget; independent context reads now run concurrently to reduce latency. Real inbound SMS/call continuity remains pending a handset-originated test.

Boundaries: contact/project/line permissions stay authoritative; custom prompts cannot authorize mutations. Incoming ambiguous project/case requests must clarify. History contains only ingested, permission-eligible sources and is not an exhaustive archive.

Live test at 2026-09-09 11:46-11:48 UTC: SMS comm_7f3bad063bd44a55b71a867824e388e8 and call comm_5be7649ae7970569d4fac7b5757dd94c shared thread_aec093237f8c49f8b29c373b531fedde. Provider delivery and signed callbacks succeeded, but the inbound SMS job waited for the daily scheduler and its unassigned source was excluded from project history. Targeted post-response processing now uses a durable job lease through Vercel waitUntil; it does not sweep unrelated queued jobs. Current-thread retrieval includes only unassigned evidence for the same person and thread. The regression fixture now uses raw provider SMS tables and verifies sender mapping, current-thread voice evidence, and targeted job-claim idempotency. Email sending policy is unchanged.
