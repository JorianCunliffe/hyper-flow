# P01 — HyperFlow ownership and authority implementation

Date: 8 September 2026. Status: implemented and locally verified; production activation pending.

Base: `2a54acb`, including the newer acceptance programme. Worktree: `C:/Users/joria/Documents/Codex/phase01-20260908/hyperflow-5`.

## Changes and verification

Per-account draft-only/authorized-send setting now applies to every organization. HyperFlow provides the owner/admin UI and REST adapter; Communications persists and enforces it, with metadata compare-and-swap. Additional SMS/voice capabilities and authenticated project checks preserve app boundaries. No memory extraction.

Validation: 436 tests, type check and production build passed. Evidence logs are in `evidence/`. Focused tests cover account defaults, saved policy, tenant isolation, role/capability rejection, caller spoofing, stale saves, preserved metadata, draft/SMS/phone paths and provider-boundary denial. Providers and persistence are mocked; live delivery is not claimed. HyperFlow browser fixture: default load, allow selection/save, re-read and member permission error verified. This is an isolated unstyled component fixture, not authenticated production UI acceptance. Svix remains pinned to 2.1.0 for the existing verifier contract.

## Scope and ownership

No memory extraction or new service. Communications retains canonical identities, threads, provider transport and existing memory/enrichment; HyperFlow owns business policy and Asks. The normative ownership/authority contract is in [BOUNDARIES.md](../architecture/BOUNDARIES.md).

The implementation uses isolated `codex/phase01-boundaries` worktrees based on current remote main. Original dirty checkouts are unchanged. Unpublished ranked-threading/register changes are reserved for Phase 02, not silently included here.

## Activation gate

Production activation remains pending. The user's clarification is resolved: draft-only is an option for all organization accounts, and the first organization is the CEO's. No special CEO ID configuration is required for the safe default.

Deploy Communications before HyperFlow. Inventory existing accounts before rollout because previously unconfigured senders now default to draft-only; preserve only existing authorized sending grants explicitly. Verify the backend client's `tenant:policy:manage`, read/write and intended channel capabilities. See the ownership contract for details. No live email, SMS, call, schema migration or production publication was performed.

## Recovery

Keep the Communications restriction active during any HyperFlow rollback. Do not widen credentials or disable restrictions to hide policy errors. Existing operation receipts remain authoritative; reconcile any uncertain accepted operation before retry. No external effects were produced by these tests.

## Main integration update

8 September 2026: both applications' Phase 01/02 PRs are merged into main. Earlier pending-review notes are historical. See [main integration record](MAIN_RELEASE.md) for CI, revisions and remaining runtime acceptance.
