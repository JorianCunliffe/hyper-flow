# P01 — HyperFlow ownership and authority implementation

Date: 8 September 2026. Status: implemented and locally verified; production activation pending.

Base: `2a54acb`, including the newer acceptance programme. Worktree: `C:/Users/joria/Documents/Codex/phase01-20260908/hyperflow-5`.

## Changes

- Backend tenant email authority ceiling at the shared Communications client, covering direct email, action runs, Ask delivery and agent/digest send fallbacks. Caller-supplied automatic/approved fields cannot grant authority.
- Explicit 403 policy outcomes; send-only tasks/Asks do not claim successful delivery. Existing mailbox drafting, SMS and voice are preserved.
- Direct task requests and email endpoint validate project membership in the authenticated organization. Local/serverless handlers agree.
- Versioned authority fixture/schema, Phase 01 OpenAPI supplement, backend configuration examples and API/ownership docs.

## Verification

| Check | Result |
|---|---|
| Fresh `npm.cmd ci --ignore-scripts` | Completed; baseline dependency advisories remain outside this focused phase |
| `npm.cmd test` | 432 passed, zero failed/skipped |
| `npm.cmd run lint` | Exit 0 |
| `npm.cmd run build` | Exit 0 |
| `git diff --check` | No whitespace errors |
| Shared contracts | Event fixtures, authority fixtures and authority schema semantically identical across both worktrees |

Local logs: [tests](evidence/P01-tests.txt), [type check](evidence/P01-lint.txt), [build](evidence/P01-build.txt). Counts differ from P00 because this isolated base excludes the unpublished Thread Register suite. No changed database rules; the P00 rules baseline remains applicable. Live HTTP/database/UI acceptance has not been substituted by these tests.

New tests exercise all email purposes without network, caller policy spoofing, configuration failure, other authorized tenants, preserved drafts/SMS/calls, unsuccessful send-only tasks/Asks and foreign-project rejection. Existing person-project/voice/review tests also pass.

## Scope and ownership

No memory extraction or new service. Communications retains canonical identities, threads, provider transport and existing memory/enrichment; HyperFlow owns business policy and Asks. The normative ownership/authority contract is in [BOUNDARIES.md](../architecture/BOUNDARIES.md).

The implementation uses isolated `codex/phase01-boundaries` worktrees based on current remote main. Original dirty checkouts are unchanged. Unpublished ranked-threading/register changes are reserved for Phase 02, not silently included here.

## Activation gate

Production is **not activated** by this phase record. The actual CEO organization/tenant ID must be configured as draft_only in both backends. HyperFlow defaults unlisted tenants to draft-only; Communications deliberately preserves unlisted independent tenants' existing behavior. A user clarification for the production organization is pending.

Deploy transport support and its tenant ceiling before the HyperFlow consumer. Confirm named API clients have only intended SMS/voice capabilities. No migration, email, SMS, call or publication was performed in this goal. No signed callback or live-provider acceptance is claimed.

## Recovery

Keep the Communications restriction active during any HyperFlow rollback. Do not widen credentials or disable restrictions to hide policy errors. Existing operation receipts remain authoritative; reconcile any uncertain accepted operation before retry. No external effects were produced by these tests.

