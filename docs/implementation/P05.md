# Phase 05 — Transcribed meetings and contact enrichment

Status: implemented, released and controlled live acceptance verified on 8 September 2026. Entry baseline: HyperFlow `70669d6` with Phase 04's live review correction, Communications `4617b7d`. See [P04 release](P04_RELEASE.md).

Implement P05.1–P05.4 using Communications' existing recordings, transcript representation, canonical contacts/threads and enrichment. HyperFlow provides scoped ingestion/review composition and converts evidence to its existing operational candidates. Generic already-transcribed upload is first; provider selections remain pending. No audio download/retranscription or new memory application.

Acceptance covers provider/version and content duplicate handling, corrected transcripts, multiple matters without a wrong merge, unknown speakers, exact-identity provenance, source instructions as data, and review through existing commitment Asks. The distinction between local assertions and live evidence is recorded below.

## Implemented and locally verified

Meetings navigation provides a reviewed text/JSON import, attendee identity evidence, separate topic/project/thread selection, source references, revision history and proposed-action review. `GET/POST /api/meetings` authenticates membership, derives tenant/actor/project scope server-side, and delegates storage to Communications. The Vercel route uses the existing communications status function, preserving the current function limit. No source mirror or new memory layer is introduced.

The cross-app SQL/HTTP test imports two topics into separate canonical threads, retries without duplication, runs existing deterministic promise extraction, imports one HyperFlow candidate, clarifies its terms, explicitly accepts its Ask, then corrects the transcript without amending accepted terms. Scope denial and zero outbound operations are asserted. Source imports do not imply business acceptance.

HyperFlow's 480 tests, type-check and production build pass; the separate Phase 05 cross-app integration passes. Communications has 262 unit and 33 SQL-backed tests passing. Raw outputs are in each checkout's `docs/implementation/evidence/P05-*` files. Browser verification of the real panel with a synthetic transport exercised form entry, import, revision display and pending/empty proposed-action state; no browser errors were observed. This fixture is not production evidence.

Assumptions: generic upload first; source-provider IDs/revisions are supplied. URLs are references only. Exact contact matches do not verify a speaker. Multi-project meetings require access to every topic. Private-source grants are not inferred. Equal-content fingerprints are not fuzzy duplicate detection. Calendar linking resolves an existing event; it does not book one.

## Release gate

Communications 2.5.0 and migration 022 were deployed before HyperFlow. Calendar/social/CMS account selection remains pending for later phases.

The live authority path required a correction: the configured service client cannot assert arbitrary user identities. HyperFlow now audits its authenticated user locally (meeting receipt and project IDs only), while Communications audits the authenticated service client. This preserves the existing grant boundary and does not mirror transcript storage. Main `e433cfb`, Vercel `dpl_V85VuK1sxw8uDQYxS9Jk5YY7gFnB`, and CI `34230005189` passed.

## Controlled production evidence

- Communications source `f83c493` is live as 2.5.0, fingerprint `b701c71e4b66`; Replit reports success. Both databases have migration 022. An unsafe generated drop was cancelled and development aligned before republishing; no history table was dropped.
- In the existing **communications test** project, source `generic / p05-release-20260908`, titled **P05 controlled release acceptance**, imported successfully. A fresh source proposal showed the actual test-speaker quote, rather than stale or fixture evidence.
- Creating the action yielded a candidate with missing owner, beneficiary and deadline. Clarification supplied self-to-self terms; a separate explicit Ask accepted the controlled obligation for 11 September 2026 at 17:00 Australia/Brisbane.
- A GET-to-editor retry of source revision 1 returned “This source revision was already imported. No duplicate evidence was created.” The list retained one meeting.
- Revision 2 corrected the first topic and added a separate topic. History displayed source revisions 1 and 2. The original thread stayed `thread_195cb6c4e34247e1bf362b3bb14d3890`; the separate matter received `thread_35d7e99169be4d0e9549bd32b31dfd11`.
- Returning to Obligations showed the original accepted wording, self-to-self owner/beneficiary and unchanged 11 September deadline. Source correction did not amend accepted operational terms.
- Both unauthenticated meeting endpoints returned 401. Final service health returned the expected fingerprint. HyperFlow logs showed successful authenticated requests; earlier release errors and startup health transients were investigated, not hidden.
- Final source checks: 480 HyperFlow tests, 262 Communications unit tests, 33 SQL-backed tests, and the separate source-to-Ask HTTP/SQL test pass. CI also passed type-check, build, dependency audit and Firebase rules. Browser fixture and live UI checks are distinct evidence.

No email, SMS, phone call, calendar booking or public publication was performed for this phase. Contact matching and cross-project denial were proved in controlled SQL fixtures; automatic third-party meeting-provider collection and real calendar/social/CMS writes are not claimed. Phase 06 can proceed.
