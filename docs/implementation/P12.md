# Phase 12 — Integrated acceptance

Status: in progress; not accepted. Phase 11 database recovery, private Sydney file storage, Ask attachments and audited operational checks are live. Core storage and Ask acceptance passed; cross-service lifecycle/restore and remaining SaaS/provider acceptance work are unfinished.

## Controlled voice check, 9 September 2026

One approved-target call (number ending 8522) was submitted with durable idempotency identity `hyperflow-p12-voice-drain-20260909-01`. The saved host receipt is `/tmp/hyperflow-p12-voice-drain-20260909.json`. No retry or second call was placed.

- Call: `CA99efa6eb8bd24ebda3dcc3c987934140`; communication: `comm_34d2ea57c06c4d318dca39ba7ef0fc8f`.
- Submission returned 201. The provider and application later reported completion, four seconds, and `machine_start`. The business outcome is failed because an answering machine was detected. The transcript is empty. This does not verify retention of the final human spoken turn.
- The event audit shows delivered start events and a failed terminal callback with HTTP 422. Inspection found inherited run/task/project identifiers from the earlier invalid acceptance thread. The legacy outbound-call route did not establish a separate purpose, allowing ordinary threading to reuse that conversation. This is a failed test setup; the receiver must retain strict workflow validation. Historical events have not been rewritten or replayed.
- Next controlled call must use the canonical communications API with an explicit new test purpose/thread, no invented workflow identifiers, and the approved target only after the user indicates readiness to answer. Verify the saved human turn, business outcome and actual terminal callback receipt independently.

## Outstanding acceptance gates

Complete Phase 11 lifecycle and recovery, then run the integrated CEO stories against exact deployed versions. Calendar booking, Google spreadsheet export and selected social/CMS publication need the previously requested targets and grants. Email remains draft-only. Provider delivery, application persistence, callback processing and business completion are separate assertions; none substitutes for another.

## User-requested repeat call — 02:38 UTC, 9 September 2026

The user explicitly requested another call. One call to the approved number ending8522 used canonical POST `/v1/calls`, idempotency key `hyperflow-p12-voice-drain-20260909-02`, explicit new thread `thread_p12_voice_20260909_0236`, and a distinct acceptance-test purpose. No workflow run/task/project identifiers were invented. The host request and receipts are saved privately at `/tmp/hyperflow-p12-voice-drain-20260909-02.json`.

Submission returned201: communication `comm_482e736a0f2e49b2abe48a0f4cfa7ec7`, provider call `CA6b250e1a469fe4d6d26ac4c8c0c2d2af`. Provider status completed after36 seconds; the application classified `human_completed`, success true, confidence0.98. The canonical `content` contains the user's spoken turns, including the final request asking the assistant to hang up. The separate `transcript` field was null; consumers must not mistake that field for absence of captured speech in this response.

The persisted `call.completed`, `transcript.completed` and `summary.completed` callbacks all show delivered, one attempt, no error. This verifies a captured final human turn and successful terminal-event delivery for this controlled call, resolving the earlier invalid-thread test setup. It does not establish who disconnected the call or prove autonomous hang-up behavior. No additional call, SMS, email or schedule operation was initiated by this test.

## Repeatable integrated acceptance - 9 September 2026

`tests/integration/phase12.test.ts` uses a real Communications HTTP fixture backed by PostgreSQL-compatible PGlite with all migrations, and the Firebase emulator with actual rules and server stores. Only the lifecycle identity verifier uses a known fixture identity; membership remains database-backed. External providers and model generation are not exercised.

The journey links Alex's email, SMS and voice to one matter, keeps an unrelated matter separate, obtains the stable contact from Communications, and accepts a HyperFlow obligation benefiting that contact. The approved weekly XLSX flow waits for artifact approval and review; competing generation attempts produce one result. Generating the report neither fulfills the promise nor sends anything. Recovery rejects suspension in the wrong order, replays the Communications receipt, separately suspends HyperFlow, checks the commitment export checksum, rejects normal work while suspended, and resumes both services while preserving the thread. Cross-tenant reads and direct artifact forgery are denied.

CI now pins Communications source to `99208f306ecab3232176eb949d16f767e08d5a67` and runs every integration scenario. The public checkout requires no new credential. Changing this pin requires acceptance again; it does not deploy Communications or merge application ownership.

| Gate | Evidence / state |
|---|---|
| Existing cross-app baseline | 12 scenarios passed on 9 September |
| Communications baseline | 263 unit and 40 database tests passed at source `99208f3` |
| Combined contact, promise, report and recovery | Passed locally with all 13 integration scenarios; release CI records the repeat |
| Split meetings, duplicates and changed source review | Existing Phase 05 integration, included in the same CI job |
| Human voice and final captured turn | Live call and delivered callbacks recorded above |
| Private storage, Ask files and diagnostics | Live evidence in `../MANAGED_FILES.md`, `../OPERATIONAL_DIAGNOSTICS.md` and P11 |
| Calendar, Google export, social/CMS | Pending selected targets and grants |
| Provider backup restore | Outstanding; local suspend/resume does not prove backup restore |
| Full CEO UI journey and measured effort | Outstanding; no time-saving claim |
| CEO final acceptance | Pending complete evidence and remaining provider cases |

No live messages are sent by this package. Email remains draft-only. Phase 12 stays in progress until mandatory live gates and CEO acceptance are complete.

### Release and recovery

This package changes tests, CI and documentation only, with no runtime, environment or schema change. Merge after both CI jobs pass and verify the production alias against the merged revision. If necessary, promote previously verified deployment `dpl_4rSDWsMco4FVi2tFw8RLu1u6iwRQ`, source `a1309a599e41949bd7d128ae5282d67816ae9bdd`. Retain current storage rules, bucket, lifecycle guard and draft-only policy. Communications keeps its separate release and migration receipt.

Live read checks repeated at approximately 07:15 UTC: storage and lifecycle guards enabled, fixture tenant active, all six fixture file receipts still deleted, member diagnostics denied403, missing reason rejected422, and two-tenant diagnostics isolation passed. Audit `17dbed84-fd48-456d-99a0-39e303258b70`. Communications is correctly unavailable for the unconfigured fixture organization; this is not evidence about the CEO organization connection.

CI setup correction: the pinned Communications lockfile contains Replit-internal package URLs. The isolated job replaces only that mirror prefix with the public npm registry, requiring and retaining each package integrity hash. Package versions are unchanged; neither Communications source nor its deployed lockfile is modified. The first CI run failed during installation, before any integration test executed.

Read-only CEO UI checks repeated: Files shows the previously uploaded report ready; Cockpit All work shows the fulfilled P04 obligation and the accepted meeting-derived Friday report with source communication `comm_f15b1733da514c558dfdfa9638c9a985`. The obligation detail retains its source link, deadline, acceptance criteria and explicit submission/review controls. No obligation was fulfilled or edited during these checks. This proves those screens load actual records, not the entire end-to-end live journey.

## CEO journey and isolated provider recovery - 9 September 2026

A new UI-driven fixture `p12-ceo-journey-20260909-0734` imported a reviewed transcript into Communications in the existing communications test project. Attendee email matched the existing Jorian contact `39edc52f-7af6-43c1-a2db-53ad43bd9ddf`; the new thread is `thread_5094b66be59648578108aba6be9ca7d9`, communication `comm_68c9f3d8db844465bda4ca42decd54c3`. Two similar extracted suggestions appeared; only one was imported. Suggested actions must remain review candidates, not automatically accepted work.

HyperFlow obligation `ob_35618cef32c17a658df2a787cc7722953332bacc13e4676f6818b5b84fc32edf` was clarified, then separately accepted through its Ask. Owner is the authenticated CEO user, beneficiary the existing contact, due 11 September at17:00 Australia/Brisbane. A separate database read confirmed accepted version3. The approved weekly XLSX flow `800494bb-a8bb-4fdb-b696-a185c0ee9b5b` created run `27f132fe43bd4b2fb86bd9467db2db96`, which correctly rejected reconciliation before file review.

Artifact `3e21bbb16a3fb358b913750104e7d5393caf817f2fd96d81ea28f3bbfc450963` froze three accepted records, two open and one fulfilled, including the new source-linked promise. The recurring flow explicitly uses a rolling seven-day period; the separate manual preparation dates do not configure it. Downloaded XLSX SHA256 `562a66c7382a95d19f4d83c6d1cf877fc7936be464a3d403f2c40b5a682dc510` matched its receipt. Recalculated totals and contact/source IDs passed, but visual inspection found clipped multiline transcript text. This file remains unapproved. The generator now counts explicit line breaks and divides newline-heavy evidence across bounded continuation rows. Empty communication-reference cells are stored as null, avoiding a preview importer displaying a shared-string index. Tests assert line visibility, complete reconstructed evidence, retained references and bounded row height. Replacement live file review remains required after deployment.

### Isolated storage restore receipt

Only `managed/org_4f5a8f3f123b4b0fb4ee4418c589dd7c/file_acceptance_1788927331962` in the Sydney bucket was restored. Original generation `1788927357676170`; successful checked generation `1788939295095262`. Restore used `ifGenerationMatch=0`, preserving any possible live replacement. The returned size2097289, MD5 `6VPPm/rudJ8tggmKWNDE2A==` and CRC32C `s/x7Lg==` match the known original fixture. HyperFlow download remained409 because its deletion receipt was retained; anonymous provider access remained403. Conditional deletion removed the restored generation and a provider read confirmed404. The measured checked round trip was6916ms, not an SLA.

The operator account can restore and delete but its raw object download was denied403. Thus this check proves provider checksum preservation and access controls, not a fresh operator byte download. Earlier diagnostic attempts also ran conditional cleanup. Original and cleanup soft-deleted versions remain under the existing retention policy; no CEO object or permission was changed. This uses Google's [object restore contract](https://docs.cloud.google.com/storage/docs/json_api/v1/objects/restore).

### Google connection readiness

The requested CEO Gmail and Google Workspace connections are present. Loading Docs/Sheets through HyperFlow succeeded, including the existing controlled coaching fixtures. Diary's Load calendars returned `Reconnect this Google account with calendar read access`. Front-end action: Diary, expand Calendar access and booking policy, Connect calendar booking access, choose the CEO Google account and complete consent. Then Load calendars, select a controlled calendar and configure the project booking policy. No calendar grant or booking was invented.
