# P00 — HyperFlow baseline and handoff

Date: 8 September 2026. Scope: read-only application baseline, safe checks and project documentation. No application source was changed, no migration applied and no communication dispatched.

## Baseline and source reconciliation

- Local HEAD: `6b9acf5`. Existing tracked edits: communications status handler, Settings modal, Communications client/types, local server, Vercel config and its test. Existing untracked Thread Register, proxy and UI/tests were preserved.
- GitHub main, fetched without updating the working tree: `2a54acbc585b51e2a699a08dfa1acbbe213f0777`. The additional commit adds `docs/COMPREHENSIVE_ACCEPTANCE_PROGRAM.md` and an operations link; it does not change application code.
- The remote acceptance programme is complementary to the local acceptance plan. Preserve both and reconcile test IDs/entry points when integrating the docs. Its full Communications test command includes live cases; baseline uses `test:unit` instead.
- Intended HyperFlow implementation checkout remains this directory. Do not reset or blindly pull over the existing work. Integrate the remote documentation commit while preserving it in the next implementation phase.
- Intended Communications implementation checkout: `C:/Users/joria/OneDrive/Documents/ChatGPT/communications-service`. See its `docs/implementation/P00.md`. The other checkout is older and should not be used to rebuild missing threading features.

## Safe verification

| Check | Result | Evidence |
|---|---|---|
| `npm.cmd test` | 434 passed, zero failed/skipped | [Test log](evidence/P00-hyperflow-tests.txt) |
| `npm.cmd run lint` | Exit 0 | [Type-check log](evidence/P00-lint.txt) |
| `npm.cmd run build` | Exit 0 | [Build log](evidence/P00-build.txt) |
| `git diff --check` | No whitespace errors; existing CRLF notices only | Command observed in phase execution |
| Firebase rules | 30 passed, zero failed; emulator exited cleanly | [Rules log](evidence/P00-rules.txt) |
| Full HTTP/database E2E | Not run; separately configured server/emulator required | `tests/e2e/README.md` |

The build regenerated ignored build output. No source edits were required for these results. Installed-dependency checks are not a clean-install reproducibility claim.

Rules ran with the Android Studio bundled Java added to this command's PATH. The CLI emitted an engine advisory for Node 25, but the emulator and all 30 checks completed successfully. Prefer the project's supported runtime for release checks.

## Deployment evidence

Vercel API identifies production deployment `dpl_2dgw2zLkPWcUSJDueodp5d5WgaNA` as READY at commit `2a54acbc585b51e2a699a08dfa1acbbe213f0777`. Public root `https://hyper-flow5.vercel.app/` returned HTTP 200. An unsigned empty POST to `/api/events` returned HTTP 401 with invalid/missing signature. This is a rejection check, not a signed ingestion or workflow acceptance test.

Local uncommitted Thread Register changes are not included in that deployed commit. Production tenant grants, mailbox health, scheduler ticks, credentials and actual database state were not accessed. Those readiness items remain unverified rather than being inferred from the root response.

## Ownership and gaps

1. Communications has the register/candidate/rethread endpoints in its newer working copy. The apparent missing backend was a checkout mismatch. Cross-service live compatibility is still unverified.
2. Existing memory remains in Communications. HyperFlow continues to reference evidence and own Asks/business state.
3. Draft-only mailbox operations coexist with other outbound email routes. CEO policy is not proven comprehensive; P01 must enforce it across actions, Ask delivery, digests and service entry points.
4. Source includes a daily Vercel fallback cron and a Communications five-minute tick helper. Actual cadence requires separate runtime evidence.
5. Outlook exists in Communications source; older specification text describing it as unavailable is stale. Source presence does not establish connected-provider health.

## Fixtures and limits

Local UI fixture uses organization `fixture`, Alpha/Beta projects and synthetic contacts. Communications database tests use an isolated in-process PGlite database and deterministic fixtures. These are local acceptance assets, not provisioned live customers.

No controlled production mailbox/phone target or live budget was selected in this phase. No production tenant was created. Before live acceptance, record exact controlled resources and applicable authority; do not use synthetic fixture numbers for dispatch. This does not block baseline documentation or the local implementation phases.

## Handoff

P00 establishes a local and available-deployment baseline, not production readiness. P01 can proceed with the recorded runtime unknowns. Preserve local work, include the newer remote changes when integrating, and do not create a separate memory layer.

Phase gate: accepted for baseline scope. Local fixtures are available; production fixture provisioning, authenticated migration/mailbox checks and live communications are explicitly not performed. These remain entry conditions for the applicable live acceptance stages, not claimed passes.

No commit/push/deployment was performed: this phase adds baseline records only and does not publish the existing unrelated/uncommitted product work. Recovery is limited to reverting these documentation additions if needed; no provider effects require reconciliation.
