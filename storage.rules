rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Managed uploads/downloads pass through authorized HyperFlow endpoints.
    // Cloud Storage IAM, not these browser rules, authorizes the server.
    match /{path=**} { allow read, write: if false; }
  }
}
